
<div class="accomplishments_members index">
<h3>Accomplishments Given to Members</h3>

<p><i> Assigning Accomplishments to Members </p></i>

<table class="table" summary="Accomplishments & Members">

    <tr class="header">
        <th>Member Info</th>
        <th>Accomplishment</th>
        <th>Semester</th>
				<th>Details</th>
        <th>Edit</th>
        <th>Delete</th>
    </tr>
    <% @accomplishments_members.each do |am| %>
    <tr>
        <td><%= "#{am.member.name} (#{am.member.email})" %></td>
        <td><%= "#{am.accomplishment.name} (#{truncate(am.accomplishment.description, length: 16, separator: /\s/)})" %></td>
        <td><%= "#{am.semester.name} (#{am.semester.dates})" %></td>
				<td>
					<%= link_to("", accomplishments_member_path(am), :class => 'fa fa-info-circle fa-lg') %>
				</td>
				<td>
					<%= link_to("", edit_accomplishments_member_path(am), :class => 'fa fa-edit fa-lg') %>
				</td>
				<td>
					<%= link_to("", delete_accomplishments_member_path(am), :class => 'fa fa-trash fa-lg') %>
				</td>
    </tr>
    <% end %>
</table>

<%= link_to("Give Accomplishment to Member", new_accomplishments_member_path, :class => "btn btn-primary") %>
<%= link_to("Download Accomplishment Data", { :controller => :accomplishments_members, :action => :download, format: 'csv'}, { :class => "btn btn-primary" } ) %>

<!-- <p>Find me in app/views/manual_points/index.html.erb</p> -->
</div>
